download-data.R
Downloads a csv file from a public URL and stores it in data

wrangle-data.R
imports a csv from data, adds a calculated column and stores it as a r object in rda folder

analysis
loads data from rda, creates a plot and saves it under figs
